server {
    listen 80;
    server_name _;

    # No static assets — route every request to the PHP front controller over FastCGI.
    location / {
        include        fastcgi_params;
        fastcgi_pass   myvideofeed:9000;
        fastcgi_param  SCRIPT_FILENAME /app/public/index.php;
    }
}
